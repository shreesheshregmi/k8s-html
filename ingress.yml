apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-k8s
spec:
  rules:
  - host: hello.k8s
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-world-service
            port:
              number: 80